```mermaid
import { useState } from "react";

const Box = ({ label, sub, color = "#4A90D9", w = 160, h = 44, textColor = "#fff" }) => (
  <div style={{
    width: w, minHeight: h, background: color, borderRadius: 8,
    display: "flex", flexDirection: "column", alignItems: "center",
    justifyContent: "center", padding: "4px 8px",
    boxShadow: "0 2px 6px rgba(0,0,0,0.18)", flexShrink: 0
  }}>
    <span style={{ color: textColor, fontWeight: 700, fontSize: 11, textAlign: "center", lineHeight: 1.3 }}>{label}</span>
    {sub && <span style={{ color: textColor === "#fff" ? "#d0eaff" : "#666", fontSize: 9.5, marginTop: 2, textAlign: "center", lineHeight: 1.2 }}>{sub}</span>}
  </div>
);

const Arrow = ({ label, color = "#999" }) => (
  <div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "1px 0" }}>
    {label && <span style={{ fontSize: 9, color, fontWeight: 600, marginBottom: 1, whiteSpace: "nowrap" }}>{label}</span>}
    <div style={{ width: 2, height: 12, background: "#bbb" }} />
    <div style={{ width: 0, height: 0, borderLeft: "5px solid transparent", borderRight: "5px solid transparent", borderTop: "7px solid #bbb" }} />
  </div>
);

const Tag = ({ t }) => (
  <span style={{ background: "#eef2ff", border: "1px solid #b0c4ee", borderRadius: 4, fontSize: 9, color: "#334da0", padding: "1px 5px", fontFamily: "monospace" }}>{t}</span>
);

const SecTitle = ({ children, color }) => (
  <div style={{ fontWeight: 800, fontSize: 14, color, borderBottom: `2px solid ${color}`, paddingBottom: 4, marginBottom: 12 }}>{children}</div>
);

// ══════════════════════════════════════════
//  WASF 旧版
// ══════════════════════════════════════════
function OldWASF() {
  return (
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
      <Box label="输入 x" sub="(B, 192, H, W)" color="#2d6a4f" w={200} />
      <Arrow />
      {/* 两路并行 */}
      <div style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
        {/* 低频路 */}
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
          <Arrow />
          <Box label="low_blur" sub="DW Conv 3×3" color="#1a6ea8" w={120} />
          <Arrow label="low (B,192,H,W)" color="#1a6ea8" />
          <Box label="low" sub="空域低频近似" color="#1a6ea8" w={120} h={34} />
        </div>
        {/* 高频路 */}
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
          <Arrow />
          <Box label="high = x − low" sub="残差≈高频" color="#7d3c98" w={128} />
          <Arrow />
          <div style={{ display: "flex", gap: 4 }}>
            <Box label="high_s" sub="DW Conv3×3" color="#9b59b6" w={90} h={38} />
            <Box label="high_m" sub="DW Conv5×5" color="#9b59b6" w={90} h={38} />
          </div>
          <Arrow label="(s+m)/2" color="#9b59b6" />
          <Box label="high_fused" sub="(B,192,H,W)" color="#7d3c98" w={128} h={34} />
        </div>
      </div>
      <Arrow />
      <Box label="α·low + (1−α)·x + β·high_fused" sub="空域加权  α,β 均为可学习标量" color="#c0392b" w={270} />
      <Arrow />
      <Box label="输出" sub="(B, 192, H, W)" color="#2d6a4f" w={200} />
      <div style={{ marginTop: 10, background: "#fff5f5", border: "1px solid #f5a0a0", borderRadius: 6, padding: "8px 10px", width: 270 }}>
        <div style={{ fontSize: 11, color: "#c0392b", fontWeight: 700, marginBottom: 4 }}>⚠ 核心问题</div>
        {["全程空域操作，没有 FFT / DWT","低频 = 卷积模糊，精度低","高频 = 残差，非频谱分量","无法实现论文中的频域λ混合"].map((s,i)=>(
          <div key={i} style={{ fontSize: 10, color: "#555", lineHeight: 1.7 }}>· {s}</div>
        ))}
      </div>
    </div>
  );
}

// ══════════════════════════════════════════
//  WASF 新版
// ══════════════════════════════════════════
function NewWASF({ n }) {
  const cfg = { 3:["k=3, wt_levels=1","小感受野"], 5:["k=5, wt_levels=2","中感受野"], 7:["k=5, wt_levels=3","大感受野"] }[n];
  return (
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
      <Box label={`输入 x  (n=${n})`} sub="(B, dim, H, W)" color="#2d6a4f" w={210} />
      <Arrow />
      {/* DWTConv 块 */}
      <div style={{ background: "#e8f4fb", border: "2px solid #2980b9", borderRadius: 10, padding: "8px 12px", width: 270, marginBottom: 2 }}>
        <div style={{ fontSize: 10, fontWeight: 800, color: "#1a5276", marginBottom: 6 }}>① DWTConv  ←  WTConv2d 官方实现</div>
        <div style={{ display: "flex", justifyContent: "space-between" }}>
          <Box label="base_conv" sub={`DW Conv ${cfg[0].split(",")[0].replace("k=","k=")} · same`} color="#2471a3" w={120} h={40} />
          <div style={{ display: "flex", alignItems: "center", fontSize: 13, color: "#888" }}>+</div>
          <Box label="wavelet_conv" sub={`Haar WT × ${cfg[0].split("levels=")[1]} 级`} color="#1a5276" w={120} h={40} />
        </div>
        <div style={{ fontSize: 9.5, color: "#555", textAlign: "center", marginTop: 6 }}>
          输出 = base_scale·base + Σ wavelet_scale·IWT(WT(x))
        </div>
        <div style={{ textAlign: "center", marginTop: 4 }}><Tag t={`(B, dim, H, W)  ${cfg[1]}`} /></div>
      </div>
      <Arrow />
      {/* SPF 块 */}
      <div style={{ background: "#fef9e7", border: "2px solid #d4ac0d", borderRadius: 10, padding: "8px 12px", width: 270, marginBottom: 2 }}>
        <div style={{ fontSize: 10, fontWeight: 800, color: "#9a7d0a", marginBottom: 6 }}>② SPF  ←  FFT 频域低/高频分离（公式5-13）</div>
        {[
          ["FFT2 + fftshift", "→ 频域复数  (B,C,H,W)", "#d4ac0d", "#333"],
          [`中心掩码 Alf  (${2*n}×${2*n}区域)`, "→ Slf 低频 / Shf 高频", "#e67e22", "#fff"],
          ["λ·Slf + (1-λ)·Shf", "λ可学习，初始0.6，偏向低频", "#ca6f1e", "#fff"],
          ["ifftshift + IFFT2", "→ 回到空域实数  (B,C,H,W)", "#d4ac0d", "#333"],
        ].map(([l,s,c,tc],i)=>(
          <div key={i} style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 4 }}>
            <Box label={l} sub={s} color={c} textColor={tc} w={185} h={36} />
          </div>
        ))}
      </div>
      <Arrow />
      <Box label="Conv1×1 + InstanceNorm + LeakyReLU" sub="通道混合" color="#6c3483" w={270} />
      <Arrow label="residual_scale · z  (初始0.1)" color="#6c3483" />
      <Box label="x  +  residual_scale · z" sub="残差连接" color="#1e8449" w={230} />
      <Arrow />
      <Box label="输出" sub="(B, dim, H, W)" color="#2d6a4f" w={210} />
      <div style={{ marginTop: 10, background: "#f0fdf4", border: "1px solid #86efac", borderRadius: 6, padding: "8px 10px", width: 270 }}>
        <div style={{ fontSize: 11, color: "#166534", fontWeight: 700, marginBottom: 4 }}>✅ 修复</div>
        {[`WTConv2d：真实Haar小波 (${cfg[0]})`,`SPF：FFT精确分离Slf/Shf`,`λ控制频率重心偏向中频区域`,`n=${n}→${cfg[1]}，PFB各分支独立`].map((s,i)=>(
          <div key={i} style={{ fontSize: 10, color: "#555", lineHeight: 1.7 }}>· {s}</div>
        ))}
      </div>
    </div>
  );
}

// ══════════════════════════════════════════
//  PFB 旧版
// ══════════════════════════════════════════
function OldPFB() {
  return (
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
      <Box label="输入" sub="(B, 1024, H, W)" color="#2d6a4f" w={230} />
      <Arrow />
      <Box label="reduce  Conv1×1 + IN + LReLU" sub="1024 → 192" color="#4a235a" w={250} />
      <Arrow label="(B, 192, H, W)" color="#4a235a" />
      <Box label="WASF（单个，所有分支共享）" sub="空域混合版" color="#c0392b" w={250} />
      <Arrow label="(B, 192, H, W)" color="#c0392b" />
      {/* 3分支 */}
      <div style={{ display: "flex", gap: 6, alignItems: "flex-start" }}>
        {[["branch1","DilConv\nrate=1","#2471a3"],["branch3","DilConv\nrate=3","#1a5276"],["branch_asym","Conv1×7\n+Conv7×1","#7d3c98"]].map(([l,s,c])=>(
          <div key={l} style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
            <Arrow />
            <Box label={l} sub={s} color={c} w={108} h={50} />
            <div style={{ fontSize: 9, color: "#777", marginTop: 3 }}>(B,192,H,W)</div>
          </div>
        ))}
      </div>
      <Arrow label="Concat → (B, 576, H, W)  [3×192]" color="#c0392b" />
      <Box label="fuse  Conv1×1 + IN + LReLU" sub="576 → 1024" color="#922b21" w={250} />
      <Arrow label="(B, 1024, H, W)" />
      <Box label="⊕ identity  Conv1×1" sub="输出 (B, 1024, H, W)" color="#1e8449" w={230} />
      <div style={{ marginTop: 10, background: "#fff5f5", border: "1px solid #f5a0a0", borderRadius: 6, padding: "8px 10px", width: 340 }}>
        <div style={{ fontSize: 11, color: "#c0392b", fontWeight: 700, marginBottom: 4 }}>⚠ 核心问题</div>
        {["WASF共享：各分支无独立频域感知","仅3分支，缺 rate=5, 7 大感受野","非对称卷积代替标准扩张卷积","Concat 576 < 论文要求的 768 (4×192)"].map((s,i)=>(
          <div key={i} style={{ fontSize: 10, color: "#555", lineHeight: 1.7 }}>· {s}</div>
        ))}
      </div>
    </div>
  );
}

// ══════════════════════════════════════════
//  PFB 新版
// ══════════════════════════════════════════
function NewPFB() {
  const branches = [
    { l:"Branch 1", rate:"rate=1", n:3, c:"#1a6ea8" },
    { l:"Branch 3", rate:"rate=3", n:5, c:"#1a5276" },
    { l:"Branch 5", rate:"rate=5", n:7, c:"#6c3483" },
    { l:"Branch 7", rate:"rate=7", n:7, c:"#4a235a" },
  ];
  return (
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
      <Box label="输入" sub="(B, 1024, H, W)" color="#2d6a4f" w={230} />
      <Arrow />
      <Box label="reduce  Conv1×1 + IN + LReLU" sub="1024 → 192" color="#4a235a" w={250} />
      <Arrow label="(B, 192, H, W)" color="#4a235a" />
      {/* 4分支 */}
      <div style={{ display: "flex", gap: 5, alignItems: "flex-start" }}>
        {branches.map((b)=>(
          <div key={b.l} style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
            <Arrow />
            <div style={{ background: b.c, borderRadius: 8, padding: "6px 5px", width: 108, boxShadow: "0 2px 6px rgba(0,0,0,0.2)" }}>
              <div style={{ color: "#fff", fontSize: 10, fontWeight: 800, textAlign: "center", marginBottom: 4 }}>{b.l}</div>
              <div style={{ background: "rgba(255,255,255,0.15)", borderRadius: 4, padding: "2px 4px", marginBottom: 3, textAlign: "center" }}>
                <div style={{ color: "#d0f0ff", fontSize: 9 }}>Conv1×1+IN</div>
              </div>
              <div style={{ background: "rgba(255,255,255,0.15)", borderRadius: 4, padding: "2px 4px", marginBottom: 3, textAlign: "center" }}>
                <div style={{ color: "#d0f0ff", fontSize: 9 }}>DilConv {b.rate}</div>
              </div>
              <div style={{ background: "#f39c12", borderRadius: 4, padding: "3px 4px", textAlign: "center" }}>
                <div style={{ color: "#fff", fontSize: 9, fontWeight: 800 }}>WASF (n={b.n})</div>
                <div style={{ color: "#ffe8a0", fontSize: 8 }}>WTConv+SPF+λ</div>
              </div>
            </div>
            <div style={{ fontSize: 9, color: "#555", marginTop: 3 }}>(B,192,H,W)</div>
          </div>
        ))}
      </div>
      <Arrow label="Concat → (B, 768, H, W)  [4×192]  ↑33%" color="#1e8449" />
      <Box label="fuse  Conv1×1 + IN + LReLU" sub="768 → 1024" color="#922b21" w={250} />
      <Arrow label="(B, 1024, H, W)" />
      <Box label="⊕ identity  Conv1×1" sub="输出 (B, 1024, H, W)" color="#1e8449" w={230} />
      <div style={{ marginTop: 10, background: "#f0fdf4", border: "1px solid #86efac", borderRadius: 6, padding: "8px 10px", width: 450 }}>
        <div style={{ fontSize: 11, color: "#166534", fontWeight: 700, marginBottom: 4 }}>✅ 修复</div>
        {["每分支内嵌独立WASF，n=3/5/7/7，感受野与频域半径协同增长","补充 rate=5, 7 两个大感受野扩张卷积分支","Concat维度 576→768（+33%），信息更丰富","完整对应论文图2(b)的4分支结构"].map((s,i)=>(
          <div key={i} style={{ fontSize: 10, color: "#555", lineHeight: 1.7 }}>· {s}</div>
        ))}
      </div>
    </div>
  );
}

// ══════════════════════════════════════════
//  主页面
// ══════════════════════════════════════════
export default function App() {
  const [tab, setTab] = useState("wasf");
  const [n, setN] = useState(3);

  return (
    <div style={{ fontFamily: "system-ui, sans-serif", background: "#f4f6fa", minHeight: "100vh", padding: "14px 10px" }}>
      <div style={{ maxWidth: 1160, margin: "0 auto" }}>

        {/* 标题 */}
        <div style={{ textAlign: "center", marginBottom: 14 }}>
          <div style={{ fontSize: 19, fontWeight: 800, color: "#1a1a2e" }}>架构对比：旧版 vs 标准化新版</div>
          <div style={{ fontSize: 12, color: "#666", marginTop: 3 }}>左：原实现（空域近似）&nbsp;|&nbsp; 右：对齐论文的标准化实现（真实频域操作）</div>
        </div>

        {/* Tab */}
        <div style={{ display: "flex", justifyContent: "center", gap: 10, marginBottom: 14 }}>
          {[["wasf","WASF 模块"],["pfb","PFB 模块"],["table","维度对比表"]].map(([t,l])=>(
            <button key={t} onClick={()=>setTab(t)} style={{
              padding: "6px 24px", borderRadius: 20, border: "none", cursor: "pointer",
              fontSize: 12, fontWeight: 700,
              background: tab===t ? "#2471a3" : "#dce8f5",
              color: tab===t ? "#fff" : "#333",
              boxShadow: tab===t ? "0 2px 8px rgba(36,113,163,0.3)" : "none"
            }}>{l}</button>
          ))}
        </div>

        {/* WASF Tab */}
        {tab === "wasf" && (
          <>
            <div style={{ display: "flex", justifyContent: "center", gap: 8, marginBottom: 12 }}>
              <span style={{ fontSize: 11, color: "#555", alignSelf: "center" }}>新版 WASF 中 n = </span>
              {[3,5,7].map(v=>(
                <button key={v} onClick={()=>setN(v)} style={{
                  padding: "3px 14px", borderRadius: 12, border: `1.5px solid ${n===v?"#e67e22":"#ccc"}`,
                  background: n===v?"#fef3e2":"#fff", color: n===v?"#b7770d":"#555",
                  cursor: "pointer", fontSize: 11, fontWeight: n===v?700:400
                }}>n = {v}</button>
              ))}
            </div>
            <div style={{ display: "flex", gap: 16, justifyContent: "center", alignItems: "flex-start", flexWrap: "wrap" }}>
              <div style={{ background: "#fff", borderRadius: 12, padding: "14px 16px", boxShadow: "0 2px 12px rgba(0,0,0,0.08)" }}>
                <SecTitle color="#c0392b">❌ 旧版 WASF（空域近似）</SecTitle>
                <OldWASF />
              </div>
              <div style={{ display: "flex", alignItems: "center", fontSize: 30, color: "#aaa", paddingTop: 80 }}>→</div>
              <div style={{ background: "#fff", borderRadius: 12, padding: "14px 16px", boxShadow: "0 2px 12px rgba(0,0,0,0.08)" }}>
                <SecTitle color="#1e8449">✅ 新版 WASF（频域标准化，n = {n}）</SecTitle>
                <NewWASF n={n} />
              </div>
            </div>
          </>
        )}

        {/* PFB Tab */}
        {tab === "pfb" && (
          <div style={{ display: "flex", gap: 16, justifyContent: "center", alignItems: "flex-start", flexWrap: "wrap" }}>
            <div style={{ background: "#fff", borderRadius: 12, padding: "14px 16px", boxShadow: "0 2px 12px rgba(0,0,0,0.08)" }}>
              <SecTitle color="#c0392b">❌ 旧版 PFB（3分支 + 共享WASF）</SecTitle>
              <OldPFB />
            </div>
            <div style={{ display: "flex", alignItems: "center", fontSize: 30, color: "#aaa", paddingTop: 100 }}>→</div>
            <div style={{ background: "#fff", borderRadius: 12, padding: "14px 16px", boxShadow: "0 2px 12px rgba(0,0,0,0.08)" }}>
              <SecTitle color="#1e8449">✅ 新版 PFB（4分支 + 独立WASF）</SecTitle>
              <NewPFB />
            </div>
          </div>
        )}

        {/* 维度对比表 */}
        {tab === "table" && (
          <div style={{ background: "#fff", borderRadius: 12, padding: "16px", boxShadow: "0 2px 12px rgba(0,0,0,0.08)" }}>
            <div style={{ fontWeight: 800, fontSize: 14, color: "#1a1a2e", marginBottom: 12 }}>📊 关键维度 & 操作对比</div>
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
              <thead>
                <tr style={{ background: "#eaf2fb" }}>
                  {["模块","位置 / 操作","旧版","新版","变化说明"].map(h=>(
                    <th key={h} style={{ padding: "7px 10px", textAlign: "left", border: "1px solid #d0dde8", fontWeight: 700 }}>{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {[
                  ["WASF","低频提取","DW Conv3×3 空域模糊","WTConv2d（Haar小波 k/levels 按n变化）","✅ 真实小波变换"],
                  ["WASF","高频提取","x − low（残差近似）","FFT低频掩码取反 → Shf","✅ 频域精确分离"],
                  ["WASF","频率混合","α·low+(1-α)·x+β·high（空域）","λ·Slf+(1-λ)·Shf → IFFT（频域）","✅ 频域λ混合，对应公式13"],
                  ["WASF","感受野配置","固定（kernel=3或5）","n=3→k3lv1 / n=5→k5lv2 / n=7→k5lv3","✅ 多尺度，随PFB分支变化"],
                  ["WASF","输入/输出维度","(B, 192, H, W)","(B, dim, H, W)  dim随分支变化","维度不变，但处理更精准"],
                  ["PFB","WASF位置","所有分支前 共享1个","每分支内部 各自独立1个（n=3/5/7/7）","✅ 各分支独立频域感知"],
                  ["PFB","分支数量","3（rate=1, 3, asym）","4（rate=1, 3, 5, 7）","✅ 补充大感受野 rate=5,7"],
                  ["PFB","Concat维度","(B, 576, H, W)  [3×192]","(B, 768, H, W)  [4×192]","✅ 信息量+33%"],
                  ["PFB","fuse输入/输出","576 → 1024","768 → 1024","参数量对应适度增加"],
                  ["PFB","输入/输出维度","(B, 1024, H, W)","(B, 1024, H, W)","保持不变，接口兼容"],
                ].map(([mod,loc,old,nw,note],i)=>(
                  <tr key={i} style={{ background: i%2===0?"#fff":"#f8fbfe" }}>
                    <td style={{ padding:"5px 10px",border:"1px solid #d0dde8",fontWeight:700,color:"#2471a3" }}>{mod}</td>
                    <td style={{ padding:"5px 10px",border:"1px solid #d0dde8",color:"#333" }}>{loc}</td>
                    <td style={{ padding:"5px 10px",border:"1px solid #d0dde8",color:"#c0392b",fontFamily:"monospace",fontSize:11 }}>{old}</td>
                    <td style={{ padding:"5px 10px",border:"1px solid #d0dde8",color:"#1e8449",fontFamily:"monospace",fontSize:11 }}>{nw}</td>
                    <td style={{ padding:"5px 10px",border:"1px solid #d0dde8",color:"#555" }}>{note}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}

      </div>
    </div>
  );
}
```